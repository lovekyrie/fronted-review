# Day 27 上线排障 checklist 执行记录

## 快速导航

| 今日 | 主题 | 核心文件 |
|------|------|----------|
| Day 27 | 上线排障 | [部署](../advanced/week2/deployment)、[错误监控](../network&broswer/error-monitoring) |

## 今日目标

- 汇总 Day 22–26 部署链路，形成一份**上线后排障 checklist**（按优先级排序）
- 列出线上 Top 10 问题 + 排查手法：白屏 / 资源 404 / 接口跨域 / 缓存错乱 / 环境变量漏 / gzip 失效 / 内网探活 / 证书 / CDN 未回源 / 旧版本用户
- 写成可以直接发给同事的 Markdown 模板

## 阅读卡点

- 白屏要区分**JS 报错**、**接口失败**、**资源加载失败**三种根因
- 缓存错乱 90% 来自 `index.html` 被强缓存，要能立刻说出排查命令
- 证书问题要看 `SSL handshake` 失败的具体错误码

## 速记卡 / 知识点

### 上线后排障 checklist

| 问题 | 典型症状 | 快速定位 | 常见修复 |
|------|----------|----------|----------|
| 白屏 | 页面空白、控制台报错 | DevTools Console、监控错误栈 | 回滚、修复 JS 异常、补兼容 |
| JS 资源 404 | `app.hash.js` 加载失败 | Network 看资源状态码 | 恢复旧资源、修正发布路径/CDN |
| CSS 丢失 | 页面无样式或错乱 | Network 看 CSS、MIME type | 修正资源路径、Nginx 类型配置 |
| 接口失败 | 页面加载中、数据为空 | Network 看 4xx/5xx、接口域名 | 修接口、网关、环境变量 |
| 跨域错误 | Console CORS 报错 | 看预检 OPTIONS 和响应头 | 补 CORS 配置或代理 |
| 缓存错乱 | 部分用户新旧混用 | 看 `index.html` 响应头和资源 hash | 修缓存头、刷新 CDN、保留旧资源 |
| 环境变量错误 | 请求到错误域名 | 查构建产物和运行时配置 | 重发正确配置或回滚 |
| gzip/CDN 异常 | 加载慢、压缩未生效 | 看 `content-encoding`、CDN 回源 | 修 Nginx/CDN 配置 |
| 证书问题 | HTTPS 访问失败 | 浏览器安全面板、证书链 | 更新证书、修域名绑定 |
| 旧版本用户 | 回滚后仍异常 | 看 service worker、浏览器缓存 | 清 SW、调整缓存策略 |

- 白屏优先三分法：JS 运行时报错、核心资源加载失败、接口或配置错误导致应用无法启动。
- 资源 404 要看是不是“新 HTML 引用了未上传的 hash 文件”或“旧 HTML 引用了已删除的旧 hash 文件”。
- 用户还看到旧页面，优先检查 `index.html` 是否被强缓存，其次看 CDN、service worker、浏览器缓存。
- 接口异常要区分网络层失败、跨域失败、认证失败、业务 5xx，不要统一说“接口挂了”。
- 环境变量错误通常表现为请求域名不对、开关状态不对、预发配置流入生产。
- 排障时先止损再根因：核心链路不可用时优先回滚或切流，复盘阶段再定位完整根因。

## 手写 / 流程图

```text
用户反馈 / 监控告警
  -> 复现：环境、账号、浏览器、地区、版本
  -> 分类：
       JS：Console、错误监控、source map
       资源：Network、404、MIME、CDN
       网络：接口状态码、CORS、认证、网关
       缓存：index.html、hash、CDN、service worker
       配置：env、config.js、域名、开关
  -> 判断影响面：全量 / 灰度 / 单租户 / 单浏览器
  -> 动作：修配置 / 刷 CDN / 回滚 / 热修
  -> 验证：关键路径 + 指标恢复
```

## 口述题

### 1. 用户反馈“打开就是白屏”，你怎么排查？

> 回答模板：我会先确认影响范围，是全量白屏还是个别用户、个别浏览器或灰度用户。然后按三类排查：第一看 Console 和错误监控，有没有 JS 运行时异常，并用 source map 还原堆栈；第二看 Network，入口 HTML、JS、CSS 是否 200，是否有 hash 资源 404、MIME 错误或 CDN 问题；第三看接口和配置，应用启动依赖的配置文件、用户信息接口、权限接口是否失败。若是核心链路全量不可用，先回滚到上一稳定版本，再继续定位根因。排障时不要只盯代码，也要看缓存、环境变量、Nginx 和 CDN。

### 2. 发了版但用户看到的还是旧页面，可能是什么原因？

> 回答模板：我会优先怀疑缓存链路。第一看 `index.html` 是否被强缓存，如果 HTML 没更新，用户自然还会加载旧 hash 资源。第二看 CDN 是否还缓存旧 HTML 或没有正确回源。第三看浏览器缓存和 service worker，尤其是 PWA 或离线缓存会拦截请求。第四看部署是否真的切到了新镜像或新静态目录，避免脚本成功但服务仍跑旧容器。修复上通常是调整 HTML 缓存头、刷新 CDN、处理 service worker，并确认新版本资源和 release 对齐。

## 5 分钟录音顺序

1. 排障三分类方法（1.5 分钟）
2. 白屏典型链路（2 分钟）
3. 缓存错乱典型链路（1.5 分钟）

## 今日复盘

1. 最容易被追问：白屏不是一个根因，要能快速拆成 JS、资源、接口、缓存、配置五类。
2. 当前短板：排障要准备具体命令或证据，例如 Network 响应头、错误监控 release、容器镜像 tag。
3. 下一次补充：Day28 要把部署全链路讲顺，并沉淀成 15 题答题本。
